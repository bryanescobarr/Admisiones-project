# Dependencias de la demo de Streamlit.
#
# Las versiones estan fijadas a proposito: son **las mismas que serializaron**
# models/modelo_produccion.joblib, y un desajuste de scikit-learn, numpy o joblib
# puede impedir deserializarlo en Streamlit Cloud. Si se regenera el artefacto con otro
# entorno, hay que actualizar estas lineas en el mismo commit.
#
# Para comprobar que el entorno local coincide con este archivo:
#   uv run python -c "import importlib.metadata as m; print(m.version('scikit-learn'), m.version('pandas'), m.version('numpy'), m.version('joblib'))"
streamlit==1.62.0
scikit-learn==1.9.0
pandas==3.0.5
numpy==2.5.2
scipy==1.18.0
pyarrow==25.0.1
joblib==1.5.3
shap==0.52.0
matplotlib==3.11.1
